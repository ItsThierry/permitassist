I have completed a full read-only verification against the exact diff, the current on-disk files, the dependency contract in `v24_decision_cells.py`, and every evidence artifact. Below are the findings ordered by severity.

## Remediation claims (all 5) — VERIFIED CORRECT

1. **Python 3.9 frozen dataclasses** ✓ — `permit_rule_engine.py:9` (`from __future__ import annotations`) makes all PEP 604 `str | None` / PEP 585 `dict[...]` hints lazy strings; every record `permit_rule_engine.py:151-261` is `@dataclass(frozen=True)` with **no `slots=True`**. Corroborated by `SCHEMA_INVARIANTS.md:20` and both import gates (`python39_import_gate.txt`, `python39_research_import_gate.txt`).
2. **Insertion-order byte parity** ✓ — `response_json_bytes` (`permit_rule_engine.py:303-311`, `sort_keys=False`) is the comparator in the cached-path (`tests/test_permit_rule_engine_part1.py:2118`) and fresh-path (`:2218`) tests and in `run_replay` (`scripts/…:1451,1456,1461`), so key-order drift would fail parity.
3. **Fresh path + cache-write parity, one model call/request** ✓ — `test_research_permit_fresh_path…:2124-2224` mocks the client/completion/`save_cache`, asserts `len(cache_writes)==2` byte-identical (`:2219-2220`) and `len(model_calls)==2` (`:2221`); shadow uses `resolve_v24_cell(force=True)` (`permit_rule_engine.py:752`) which adds no model call.
4. **Package-manifest SHA pinning** ✓ — `_source_package_manifest_sha256` (`permit_rule_engine.py:318-328`) hashes the manifest file when the resolver omits it (it always does — `V24Resolution.package_manifest_sha256` defaults `None`, `v24_decision_cells.py:654`); asserted at `tests/…:1870-1872`.
5. **Live post-reconciliation capture** ✓ — both return paths snapshot `deepcopy(...)` after `reconcile_authoritative_result` and before `ensure_required_filing_rows`, passing it as `authoritative_result` (`research_engine.py:9064-9067`/`:10059-10062` + observe calls); `deepcopy` is imported (`research_engine.py:18`), so `POST_RECONCILIATION_MUTATION` is genuinely reachable live, not silently `None`.

## P0/P1 blocker checklist — ALL SATISFIED in code
Off-by-default & v24-independent (`permit_rule_engine.py:344-346,739-752`; test `:1902-1911`); exception isolation (`research_engine.py:8981-8989`, observe try/except; sink absolute-path-only `permit_rule_engine.py:1114-1128`); no customer/cache/regulated mutation (envelope never merged — test `:2053-2054`; cache writes identical); **request-time-equivalent validation** — `request_time_validation` uses `validate_v24_cell(strict_snapshots=False, require_live_url_check=False)` (`permit_rule_engine.py:457-461`) matching the resolver **exactly** (`v24_decision_cells.py:766`); all 2,162 cells once (`scripts/…:1544-1570`; `summary.json` cell_count 2162, honest 36 EXACT_COMPLETE vs 2128 legacy TIER1_COMPLETE); strict ten-lane commercial-TI (`permit_rule_engine.py:56-73`); determinism identical (`generation_determinism.json`); 10 locked classes implemented + unit-executable (`tests/…:1981-2046`); package/top-level dual imports (`permit_rule_engine.py:21-38`, `research_engine.py` diff 9-18); no Part 2 surface (test `:2255-2264`).

## BLOCKERS — mandatory-gate artifacts absent

**[HIGH] Secret-scan gate has no artifact.** `secret_scan_disposition.json` (the file this review was told to read) does **not exist anywhere in the repo** (confirmed via Glob `**/secret_scan_disposition.json`, repo-wide grep, and `*secret*`/gitleaks/trufflehog scans of the artifact dir — all empty). The LOCKED plan requires "test output and secret scan" as a required Part 1 artifact (`LOCKED_FOUR_SESSION_PLAN.md:79`) and gate (`:91`), and mandates "every mandatory gate … satisfied with real artifacts" (`:178`). *Note: I inspected every added diff line — no secrets are present (only `example.test`/public muni.org URLs), so the risk is nil and remediation is cheap, but the mandatory gate is unevidenced.*

**[MEDIUM] Focused / current test-pass log not preserved.** No `tests/test_permit_rule_engine_part1` pass log exists (repo-wide grep empty), and `tests_suite_latest.txt` — the "current_log" the regression comparison is built from (`baseline_failure_comparison.json:47`) — does **not exist** (`**/tests_suite_latest.txt` empty). Only the base-SHA baseline log and the derived comparison JSON remain, so the "focused tests pass" gate (`:91`) and the *current* side of baseline-vs-regression separation are asserted but not backed by a readable artifact.

## MINOR (non-blocking)
- `summary.json` locked_taxonomy `STALE_OR_CONFLICTING_RULE: 0` — reachable only via the forced `replace(...)` unit test (`tests/…:2034-2041`); the shipped corpus has zero validation failures, so it never fires organically. Acceptable (executability is proven), worth noting.
- `SCHEMA_INVARIANTS.md:75` says replay compares "canonical" bytes, but `run_replay` correctly uses insertion-order `response_json_bytes` — doc wording is stale; code is stricter/correct.

## Assessment
The diff is technically sound, customer-safe, and every one of the five remediations plus all enumerated code-level P0/P1 blockers is verified correct. However, two **plan-mandatory Part 1 gate artifacts (secret scan; current/focused test output)** are entirely absent, and the LOCKED plan forbids certifying the checkpoint complete without real artifacts for every mandatory gate. Remediation is cheap and non-code: run the secret scan → write `secret_scan_disposition.json`; run `pytest tests/test_permit_rule_engine_part1.py` (and the broad suite) → preserve `tests_suite_latest.txt`; then re-run this review.

BLOCK_PART1_CHECKPOINT
